mixin superBg(section)
  -
    var {id, who} = section 
    .background(
      style=`
      --bgi-mobile: url(${baseUrl}asset/image/background/${id}-mobile.jpg);
      --bgi-desktop: url(${baseUrl}asset/iamge/background/${id}.jpg);
      `
    )
      h1.za-panou = who 

      .za-footer I exist, therefor I am 
